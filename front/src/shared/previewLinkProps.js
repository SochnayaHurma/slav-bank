export const PREVIEW_LINK_PROPS = {
  onClick: (event) => {
    event.preventDefault();
  },
  onAuxClick: (event) => {
    event.preventDefault();
  },
  onDragStart: (event) => {
    event.preventDefault();
  },
};
