(() => { if (window.__sbV3MenuInit) return; window.__sbV3MenuInit = true; const root = document.querySelector('[data-v3-mobile-nav]'); if (!root) return; const btn = root.querySelector('.v3-menu-btn'); const panel = root.querySelector('.v3-mobile-menu'); const backdrop = root.querySelector('.v3-mobile-backdrop'); const closeBtn = root.querySelector('.v3-mobile-close'); if (!btn || !panel || !backdrop || !closeBtn) return; const isMobile = () => window.matchMedia('(max-width: 760px)').matches; const closeMenu = () => { root.classList.remove('v3-menu-open'); document.body.classList.remove('v3-lock'); btn.setAttribute('aria-expanded', 'false'); panel.setAttribute('aria-hidden', 'true'); }; const openMenu = () => { if (!isMobile()) return; root.classList.add('v3-menu-open'); document.body.classList.add('v3-lock'); btn.setAttribute('aria-expanded', 'true'); panel.setAttribute('aria-hidden', 'false'); window.requestAnimationFrame(() => closeBtn.focus()); }; btn.addEventListener('click', () => root.classList.contains('v3-menu-open') ? closeMenu() : openMenu()); closeBtn.addEventListener('click', closeMenu); backdrop.addEventListener('click', closeMenu); panel.querySelectorAll('a').forEach((link) => link.addEventListener('click', closeMenu)); document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeMenu(); }); window.addEventListener('resize', () => { if (!isMobile()) closeMenu(); }); })();